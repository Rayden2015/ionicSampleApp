import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.nurudin.ionicAppd',
  appName: 'ionicApp',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
